<Window x:Class="WPF_varosok.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_varosok"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="500" FontFamily="16" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Magyarország városai"
               Grid.Row="0"
               Grid.Column="0"
               Grid.ColumnSpan="2"
               HorizontalAlignment="Center"
               FontSize="20"
               FontWeight="Bold"/>
        <ListBox Name="lb_varosok"
                 Grid.Row="1"
                 Grid.Column="0"
                 Height="700"
                 Margin="5"/>
        <StackPanel Orientation="Vertical"
                    Margin="5"
                    Grid.Row="1"
                    Grid.Column="1"
                    VerticalAlignment="Center">
            <TextBlock Name="tbl_nev"
                       Text="Város:"/>
            <TextBlock Name="tbl_jaras"
                       Text="Járás:"/>
        </StackPanel>
        
    </Grid>
</Window>
